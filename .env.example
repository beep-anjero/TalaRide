# Phase 1 needs no environment variables or credentials.
# Future EXPO_PUBLIC_* variables are bundled into the app and must never contain secrets.
# Copy this file to .env when a later approved phase requires configuration.
